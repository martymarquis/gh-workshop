# Publishing workflow example

The workflow in this directory is intentionally inactive. GitHub only runs workflow files stored in `.github/workflows/`.

During the workshop, create a branch and copy `publish.yml` to:

```text
.github/workflows/publish.yml
```

Then commit the workflow, push the branch, open a pull request, review the YAML, and merge it into `main`.
